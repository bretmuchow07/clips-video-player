{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": ".deploy"
            }
        }
    ],
    "buildCommand": "npm run predeploy",
    "outputDirectory": ".deploy",
    "framework": null
}